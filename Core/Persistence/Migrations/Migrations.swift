import Foundation

/// schema 迁移集合（占位，M1-011 起逐个新增）。
/// 首版表：threads / messages / branches / branch_notes
/// + 工程字段 sequence / status / updated_at / metadata_json / merge_note_id。
public enum MigrationsNamespace {}
