# G1 验收报告（B-M1，任务 M1-014）

> 日期：2026-08-01 ｜ 基线：CLI 0.31.0 / ACP v1 / acp-swift-sdk @ b800b3f / GRDB 7.11.1
> 依据：TODO.md Gate G1 验收场景（流程文档 §5.10），逐条执行。

## 自动化测试总览

`swift test`：**57 用例 / 10 套件全绿**（2026-08-01，merge 前最后一次全量）。
另有真实 CLI 集成验收套件 `RealCLIIntegrationTests`（消耗会员额度，默认跳过，
`TWIG_REAL_CLI=1 swift test --filter RealCLI` 开启），本次验收已执行 1 用例通过（8.2s）。

## 逐条证据

| 场景 | 结果 | 证据 |
|---|---|---|
| G1-01 CLI 已装已登录 → 可对话 | ✅ | 真实 CLI 握手日志 `Connected to agent: Kimi Code CLI v0.31.0`（2026-08-01 多次冒烟）；用户交互发送「你好」收到完整回复 |
| G1-02 CLI 缺失 → 安装引导，不崩溃 | ✅ | 假环境冒烟（`TWIG_HOME=/tmp/twig-g1-missing`）：日志 `进入引导页：cliMissing`，应用无崩溃，页面含官方安装命令 |
| G1-03 CLI 不支持 acp/版本不兼容 → 提示 | ✅ | 假环境冒烟（伪造 0.20.0）：日志 `unsupportedVersion(found: "0.20.0")`，页面提示 `kimi upgrade` 与基线 0.31.0 |
| G1-04 登录失效 → 登录引导 | ✅（含限制） | 假环境冒烟（无凭据）：日志 `loginRequired`，页面提示 `kimi` + `/login`。**限制**：凭据文件在但登录过期的路径走协议错误关键词兜底（`StartupIssue.isAuthRelated`），真实失效错误形态未采样（G0 待验证项），验收时如实记录 |
| G1-05 创建主线程发消息 → 连续流式 | ✅ | 用户交互实测（2026-08-01）；RealCLI 集成测试两线程均收到完整流式并 completed |
| G1-06 流式中切线程再切回 → 写入正确线程 | ✅ | 单测 `跨线程路由：流式中切线程，事件仍写入原线程`；RealCLI 实测：线程1 流式中切线程2 并发发送，最终「苹果」归线程1、「香蕉」归线程2，无交叉。**UI 限制**：B-M1 无线程列表，「切回」能力由 `switchToThread` 提供、UI 入口归 M4-007 |
| G1-07 流式中杀子进程 → 中断标记，可恢复 | ✅ | SIGKILL 冒烟（2026-08-01 16:08）：supervisor 检出异常退出 → `全部 session 映射已标记失效` + 流式消息 interrupted（保留已收内容）；单测 `进程中断：全部线程的流式消息一并标记 interrupted` |
| G1-08 重启子进程 → 可新 session | ✅ | 同一 SIGKILL 冒烟：自动重启 → 二次握手成功 → `session 已全部重建`；旧 session 不做续接假象（续接策略归 M4-010）。手动路径：重启预算耗尽后横幅提供「重新连接」 |
| G1-09 退出重开应用 → 本地线程消息仍在 | ✅ | 用户实测（2026-08-01）：重启应用后先前对话完整保留（GRDB 文件库 Application Support） |
| G1-10 未知协议事件 → 不崩溃，保守记录 | ✅ | 单测 `ACPEventAdapterTests` 未知 update 映射为 `AgentEvent.unknown` + 警告日志；真实冒烟中 SDK「unknown request」响应仅警告不崩溃 |

## 测试矩阵覆盖说明

- 消息状态机（发送即存/占位/追加/完成/失败分类/中断/重试/禁发/跨线程/中断恢复/session 重建）：`ConversationStoreTests` 9 用例；
- 启动三态映射与登录关键词兜底：`StartupIssueTests` 2 用例；
- 子进程生命周期（优雅停止/崩溃重启退避/SIGKILL 升级/幂等 start）：`ACPProcessSupervisorTests`（`.serialized`，真实派生进程）；
- 协议适配（含未知事件、稀疏 tool_call_update）：`ACPEventAdapterTests`、`ACPClientIntegrationTests`（fake agent + G0 脱敏样本 fixtures）；
- 持久化（migration 幂等/事务回滚/外键/映射回环/消息生命周期）：`PersistenceTests`。

## 结论

G1 十条全部通过（G1-04 附上述限制说明），**Gate G1 达成，B-M1 完成**。
下一阶段 B-M2（工具调用、权限策略与富文本渲染），入口任务 15（M2-001）。
