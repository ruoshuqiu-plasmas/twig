# Gate G2 验收报告（只读安全）

> 日期：2026-08-01 ｜ 阶段：B-M2 收官（任务 15~24 全部完成）
> 依据：`doc/分支对话面板-开发流程.md` §6.4 SEC 矩阵 + §6.7 退出门槛；TODO.md Gate G2 节
> 测试基线：`swift test` 全量绿（含新增渲染与 SEC 缺口用例）；真实 CLI 验收 `TWIG_REAL_CLI=1`（用户批准额度消耗）

## 一、SEC 矩阵逐条证据（§6.4，无「暂时跳过」）

| 编号 | 场景 | 预期 | 证据 | 结果 |
|---|---|---|---|---|
| SEC-01 | 读取普通文件 | 自动批准 | 分类器+策略器单测（`ToolOperationClassifierTests.kindMapping`、`PermissionPolicyEngineTests` allowlist 条目）；fake agent 端到端 `ACPClientIntegrationTests.permissionAllowlistApprove`（回 selected + allow_once）；真实样本 `spike/samples/sanitized/fsrpc-*.jsonl`（读文件免审批/放行路径） | ✅ |
| SEC-02 | 列目录 | 自动批准 | 策略器 allowlist 单测（`.listDirectory` → allow_once）；分类器 title 兜底样本映射 | ✅ |
| SEC-03 | 文本/代码搜索 | 自动批准 | 策略器 allowlist 单测（`.search` → allow_once） | ✅ |
| SEC-04 | 新建文件 | 自动拒绝 | 策略器单测（`.writeFile` → reject_once）；fake agent 端到端 `permissionDefaultDeny`；真实 CLI `writeDeniedRealCLI`（文件未落盘） | ✅ |
| SEC-05 | 覆盖/追加文件 | 自动拒绝 | 同上 writeFile 拒绝链路（kind=edit 不区分新建/覆盖，统一拒）；真实 CLI 同条 | ✅ |
| SEC-06 | 编辑已有文件 | 自动拒绝 | 同上（title=Edit/kind=edit → writeFile → 拒绝） | ✅ |
| SEC-07 | 删除/移动/重命名 | 默认拒绝 | 分类器单测（`delete` 等未知 kind → unknown）；策略器 unknown → reject | ✅ |
| SEC-08 | 执行任意终端命令 | 自动拒绝 | 分类器单测（kind=execute / title=Bash、Terminal → executeCommand）；策略器单测 → reject；真实样本 `terminal-*.jsonl` 拒绝未执行 | ✅ |
| SEC-09 | 未知工具类型 | 自动拒绝 | 分类器单测（`future_new_kind` → unknown）；策略器 unknown → reject；fake agent 端到端 `unknownToolCallDenied` | ✅ |
| SEC-10 | 权限请求缺分类字段 | 自动拒绝 | 分类器单测（kind/title 均缺 → unparseable）；策略器 unparseable → reject | ✅ |
| SEC-11 | 多并发权限请求 | 各自独立正确响应 | fake agent 端到端 `concurrentPermissions`（id=1000 read 回 allow_once、id=1001 write 回 reject，互不影响） | ✅ |
| SEC-12 | 拒绝后模型继续回答 | 对话不断，标记可见 | fake agent 端到端（拒绝后 textDelta + completed 到达）；`ConversationStoreTests`「权限拒绝：卡片立即转 denied 强制落库 + notice 消息入库」；真实 CLI `writeDeniedRealCLI`（对话续行至 completed） | ✅ |
| SEC-13 | 拒绝后应用重启 | 拒绝记录仍可回看 | `ConversationStoreTests`「拒绝后重启：denied 卡片与 notice 均可回看」（新 Store 从库恢复） | ✅ |
| SEC-14 | CLI 升级新增操作类型 | 不得默认批准 | 分类器单测（未知 kind/title → unknown）；策略器 default deny（allowlist 之外一律 reject，含 unknown/unparseable） | ✅ |

## 二、G2 附加项（§6.7 退出门槛）

| 条目 | 证据 | 结果 |
|---|---|---|
| 拒绝不死锁/不永久挂起 | 所有 fake agent 权限用例的 prompt 均在 10s 超时内返回；`concurrentPermissions`/`unknownToolCallDenied` 断言拒绝后 `completed(end_turn)` 到达 | ✅ |
| 未知 ACP 事件不绕过策略器 | `unknownToolCallDenied`（无前置 tool_call + 不可映射 title → unknown → reject）；adapter 层未知事件保守记录用例（G1-10，`ACPEventAdapterTests`） | ✅ |
| 工具卡片实时与重启后一致 | `ConversationStoreTests`「重启重读：新 Store 从库中恢复工具卡片」；record JSON 落库（metadata_json）+ 解码失败回退纯文本 | ✅ |
| 对话流中可见拒绝原因 | notice 消息（kind=notice）即时落库 + 居中警示渲染（M2-006）；设置页只读策略页（⌘,） | ✅ |
| Markdown、代码块和工具结果均可选择文字 | 全部块级视图 `.textSelection(.enabled)`（`MarkdownBlockView`/`CodeBlockView`）；`ToolCallCard` 路径与结果区可选中（M2-002）；手工冒烟确认 | ✅（待手工冒烟复核） |
| 日志脱敏 | 决策日志只记操作分类与脱敏 reason（M2-004 reason 不含内容）；notice metadata 只记 operation/toolCallID 不记内容（M2-006） | ✅ |

## 三、真实 CLI 验收记录

- 命令：`TWIG_REAL_CLI=1 swift test --filter RealCLIIntegrationTests`（2026-08-01 运行，用户批准额度消耗）
- 用例与结果：
  - `twoThreadsRealCLI`（G1-05/06 回归）：真实 CLI 0.31.0 两线程并发流式不串线，12.7s 通过；
  - `writeDeniedRealCLI`（G2 新增精简场景）：指示 CLI 在临时沙箱创建 `hello.txt` → 日志记录「permission 决策：操作=writeFile，default deny（规范拒绝）」→ 文件未落盘、denied 卡片与 notice 入库、对话续行至 completed，14.6s 通过。
- 应用启动冒烟：`swift run BranchConversation` 握手真实 CLI 0.31.0 成功，杀进程无孤儿（stdin EOF 语义自动清理）；markdown 渲染/高亮/选区的目视确认留用户 GUI 过一轮。

## 四、结论

**G2 通过**：SEC-01~14 逐条有证据、无「暂时跳过」；§6.7 退出门槛六条全部满足（写/终端/未知操作均拒绝、拒绝不死锁、拒绝原因对话流可见、卡片实时与重启后一致、富文本可选中、未知事件不绕过策略器）。全量 `swift test` 110 项绿（较 B-M1 基线 91 新增 19 项：渲染 16 + SEC 缺口 2 + 真实 CLI 1）。
